{
  "name": "pono",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @pono/web dev",
    "build": "pnpm --filter @pono/web build",
    "typecheck": "pnpm --filter @pono/web typecheck",
    "api:dev": "uv run --package pono-api uvicorn pono_api.main:app --app-dir apps/api/src --reload --port 8000",
    "api:check": "uv run ruff check apps/api && uv run ruff format --check apps/api && uv run mypy && uv run pytest",
    "sdk:generate": "pnpm --filter @pono/sdk generate",
    "check:all": "pnpm build && pnpm typecheck && pnpm api:check"
  }
}
